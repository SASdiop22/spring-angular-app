@ -1,30 +1,32 @@
# url of the development frontend
app.dev.frontend.local=http://localhost:4200

# swagger-ui custom path, this is the default path. I added the property only for show case
springdoc.swagger-ui.path=/swagger-ui.html


#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true

#logging.level.org.hibernate.SQL=DEBUG
#logging.level.org.hibernate.orm.jdbc.bind=TRACE

#jwt.secret=123456
management.endpoints.web.base-path=/actuator
management.endpoint.health.show-details=always
management.endpoints.web.exposure.include=*


# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/angular-app
spring.datasource.username=abdousamad
spring.datasource.password=
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate : ddl-auto update permet de creer/modifier les tables automatiquement
#spring.jpa.hibernate.ddl-auto=update
# Pour reinitialiser la base a chaque demarrage (utile en dev)
spring.jpa.hibernate.ddl-auto=create-drop

# Les données initiales sont créées par le seeder JobOfferSeeder.java
# Ne pas utiliser data.sql car il s'exécute avant la création des tables Hibernate

# Affichage des requetes SQL dans la console pour le debuggage
spring.jpa.show-sql=true
# Multipart upload configuration (CV upload)
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Debug Spring Security
spring.security.debug=true
logging.level.org.springframework.security=DEBUG

#Config du modele de recommandation IA dans AiMatchingServiceImpl.java > matchCvWithJob()
